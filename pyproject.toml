[project]
name = "animal-chess"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "podsixnet>=0.11.0",
]
[tool.pyright]
venvPath = "."
venv = ".venv"
include = [
  "src/*"
]
